{
  "name": "codeunicorn-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:push": "pnpm --filter @codeunicorn/database db:push",
    "db:generate": "pnpm --filter @codeunicorn/database generate",
    "db:studio": "pnpm --filter @codeunicorn/database studio"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
