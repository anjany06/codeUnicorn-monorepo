apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: codeunicorn
  labels:
    app: codeunicorn
    component: api
spec:
  selector:
    app: codeunicorn
    component: api
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
  type: ClusterIP
